1) Escreva o passo a passo Alice-Bob para implementarmos o seguinte statement.
	-Bob gostaria de enviar um documento assinado e com criptografia assimetrica para alice

Bob-
Recebe PubA de Alice
Cria as chaves: PubB e PrivB
Passa PubB para Alice
HASH = hash(sha1, documento())
SIGN = sign(PrivB, HASH, sha1)
MSG = append(documento(), SIGN)
FINAL = cript(rsa, PubA, MSG)
send(alice, FINAL)

Alice-
Cria as chaves: PubA e PrivA
Passa PubA para Bob
Recebe PubB de Bob
Recebe FINAL
READ = decrypt(rsa, PrivA, FINAL)
if (READ == NULL)
	PROBLEMA
else
	SIGN = pega_final(READ)
	BODY = pega_corpo(READ)
	HASH = hash(sha1, BODY)
	RESULT = verify(PubB, HASH, SIGN, sha1)
	if RESULT
		PASS
	else
		PROBLEMA
